{
  "name": "symphony-elixir-claude-bridge",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "claude-app-server": "node ./scripts/claude_code_app_server.mjs",
    "claude-cli-bridge": "node ./scripts/claude_code_cli_bridge.mjs",
    "dashboard:install": "npm --prefix ./assets ci",
    "dashboard:dev": "npm --prefix ./assets run dev",
    "dashboard:build": "npm --prefix ./assets run build",
    "dashboard:test": "npm --prefix ./assets run test"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.71"
  }
}
